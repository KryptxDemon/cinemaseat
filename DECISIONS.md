# Decisions — devops branch

Branch-local decisions (CI provider, deploy target, load-test shape)
are tracked inside the `devops/` subdirectory and the workflow YAML.

Cross-branch decisions are recorded on `main` after the merge.
