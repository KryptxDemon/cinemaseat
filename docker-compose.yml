# CinemaSeat root docker-compose.yml
#
# This file is a pointer stub on the `devops` branch.
# The full integration stack lives in devops/docker-compose.yml.
# See BRANCHES.md for branch ownership and the merge story.
#
# To run the full local stack from this branch:
#   docker compose -f devops/docker-compose.yml up