# CinemaSeat — backend branch

This branch implements the FastAPI booking service. See
[`backend/README.md`](./backend/README.md) for endpoint docs and
[`BRANCHES.md`](./BRANCHES.md) for branch ownership.

The root `README.md` is finalized on the `main` branch after the
last PR is merged.